#import "deps.typ": *

#let operations = shorthands.with(
  ($@$, $compose$),
  ($*$, $dot.op$),
  ($%$, $mod$),
  ($**$, $ast.basic$),
  ($\\$, $without$),
  ($==$, $equiv$),
  ($+-$, $plus.minus$),
  ($..$, $quad$),
  ($....$, $quad quad$),
)
